
const state = {
  input: {
    // status: {
    //   isNew: false,
    //   isEdit: false
    // }
  }
}

const getters = {
  // getStatus: state => {
  //   return state.input.status
  // }
}

const mutations = {
  // setStatus (state, data) {
  //   // alert('tayo')
  //   if (data.status === 'new') {
  //     state.input.status.isNew = data.value
  //     state.input.status.isEdit = false
  //   } else if (data.status === 'edit') {
  //     state.input.status.isNew = true
  //     state.input.status.isEdit = data.value
  //   } else if (data.status === 'cancel') {
  //     state.input.status.isNew = false
  //     state.input.status.isEdit = false
  //   }
  // }
}

export default {
  state,
  getters,
  mutations
}
